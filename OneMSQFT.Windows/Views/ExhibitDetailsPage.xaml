<omsqft:BasePageView
    x:Class="OneMSQFT.Windows.Views.ExhibitDetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OneMSQFT.Windows.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="using:Microsoft.Practices.Prism.StoreApps"
    xmlns:omsqft="using:OneMSQFT.Windows.Views"
    xmlns:viewModels="using:OneMSQFT.UILogic.ViewModels"
    xmlns:designViewModels="using:OneMSQFT.Windows.DesignViewModels"
    mc:Ignorable="d"
    prism:ViewModelLocator.AutoWireViewModel="True"
	d:DataContext="{d:DesignInstance designViewModels:DesignExhibitDetailsPageViewModel, IsDesignTimeCreatable=True}">

    <omsqft:BasePageView.Resources>
    	<DataTemplate x:Key="PhotoCarouselTemplate">
    		<Grid>
				<Image 
					Stretch="UniformToFill"
        			Width="{Binding ExhibitItemWidth}" 
        			Height="{Binding ExhibitItemHeight}" Source="{Binding}"/>
			</Grid>
    	</DataTemplate>
    </omsqft:BasePageView.Resources>

    <omsqft:BasePageView.BottomAppBar>
        <AppBar>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal">
                    <AppBarButton x:Name="HomeButton" HorizontalAlignment="Stretch" Label="Home" VerticalAlignment="Stretch" Icon="Home"/>
                    <AppBarButton x:Name="AboutButton" HorizontalAlignment="Stretch" Label="About" VerticalAlignment="Stretch" Icon="More"/>
                </StackPanel>
                <StackPanel Grid.Column="1" HorizontalAlignment="Right" Orientation="Horizontal">
                    <AppBarToggleButton x:Name="AdminButton" HorizontalAlignment="Stretch" Label="Admin" VerticalAlignment="Stretch" Icon="Admin"/>
                </StackPanel>
            </Grid>
        </AppBar>
    </omsqft:BasePageView.BottomAppBar>	
	<Grid Background="White">
    		<StackPanel Orientation="Horizontal">
    		<Button x:Name="backButton" HorizontalAlignment="Left"
    			Click="GoBack" 
    			IsEnabled="{Binding Frame.CanGoBack, ElementName=pageRoot}"
    			Style="{StaticResource BackButtonStyle}" Margin="100" VerticalAlignment="Top" />
			<TextBlock HorizontalAlignment="Left" Margin="100" Text="{Binding ExhibitTitleText}" VerticalAlignment="Top" Foreground="Black" FontSize="24"/>
    			</StackPanel>
        <ScrollViewer Style="{StaticResource HorizontalScrollViewerStyle}" >
        		<StackPanel Orientation="Horizontal">
        		<Image 
        			Width="{Binding ExhibitItemWidth}" 
        			Height="{Binding ExhibitItemHeight}" 
        			Source="{Binding HeroPhotoPath}" Stretch="UniformToFill"/>
        		<Grid 
        			Width="{Binding ExhibitItemWidth}" 
        			Height="{Binding ExhibitItemHeight}" Background="{StaticResource OneMSQFTWhiteBrush}" >
        			<Grid.ColumnDefinitions>
        				<ColumnDefinition/>
        				<ColumnDefinition Width="3*"/>
        				<ColumnDefinition/>
        			</Grid.ColumnDefinitions>
        			<Grid.RowDefinitions>
        				<RowDefinition/>
        				<RowDefinition Height="3*"/>
        				<RowDefinition/>
        			</Grid.RowDefinitions>
        			<Image Grid.Row="1" Grid.Column="1"
        			Source="{Binding HeroPhotoPath}" Stretch="UniformToFill"/>
        			<TextBlock HorizontalAlignment="Center" TextWrapping="Wrap" Text="{Binding ExhibitHeroText}" VerticalAlignment="Top" Foreground="{StaticResource OneMSQFTBlackBrush}" FontSize="29.333" Grid.Row="2" Grid.Column="1"/>
        		</Grid>
        		<Grid 
        			Width="{Binding ExhibitItemWidth}" 
        			Height="{Binding ExhibitItemHeight}" Background="{StaticResource OneMSQFTWhiteBrush}" >
        			<ListView HorizontalAlignment="Left" 
					VerticalAlignment="Top" 
        			Width="{Binding ExhibitItemWidth}" 
					ItemsSource="{Binding PhotoCollection}" ItemTemplate="{StaticResource PhotoCarouselTemplate}" IsSwipeEnabled="False" SelectionMode="None"/>
        		</Grid>
        		<Grid 
        			Width="{Binding ExhibitItemWidth}" 
        			Height="{Binding ExhibitItemHeight}" Background="{Binding EventColor, Mode=OneWay}" >
        			<Grid.ColumnDefinitions>
        				<ColumnDefinition/>
        				<ColumnDefinition Width="5*"/>
        				<ColumnDefinition/>
        			</Grid.ColumnDefinitions>
        			<Grid.RowDefinitions>
        				<RowDefinition/>
        				<RowDefinition Height="5*"/>
        				<RowDefinition/>
        			</Grid.RowDefinitions>
        			<TextBlock HorizontalAlignment="Center" TextWrapping="Wrap" Text="{Binding ExhibitHeroText}" VerticalAlignment="Top" Foreground="{StaticResource OneMSQFTWhiteBrush}" FontSize="48" Grid.Row="2" Grid.Column="1"/>
        		</Grid>
        		<Grid 
        			Width="{Binding ExhibitItemWidth}" 
        			Height="{Binding ExhibitItemHeight}" Background="{Binding EventColor, Mode=OneWay}" >
        			<Grid.RowDefinitions>
        				<RowDefinition Height="2*"/>
        				<RowDefinition Height="3*"/>
        				<RowDefinition Height="3*"/>
        			</Grid.RowDefinitions>
        			<TextBlock HorizontalAlignment="Center" TextWrapping="Wrap" VerticalAlignment="Bottom" Foreground="{StaticResource OneMSQFTWhiteBrush}" FontSize="48" Grid.Column="1" Text="Next"/>
        			<TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" VerticalAlignment="Top" Foreground="{StaticResource OneMSQFTWhiteBrush}" FontSize="26.667" Grid.Row="1" Grid.Column="1" Text="Next" Grid.ColumnSpan="2" Margin="0.348,30,-0.349,0" FontWeight="Light"/>
        		</Grid>
        	</StackPanel>
        </ScrollViewer>

        <VisualStateManager.VisualStateGroups>

            <VisualStateGroup x:Name="ApplicationViewStates">
                <VisualState x:Name="FullScreenLandscape" />
                <VisualState x:Name="Filled" />

                <VisualState x:Name="FullScreenPortrait">
                    <Storyboard>
                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="backButton" Storyboard.TargetProperty="Style">
                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource PortraitBackButtonStyle}" />
                        </ObjectAnimationUsingKeyFrames>
                    </Storyboard>
                </VisualState>

                <VisualState x:Name="Snapped"/>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</omsqft:BasePageView>
